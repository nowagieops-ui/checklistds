# MySQL — from Hostinger hPanel > Databases
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=

# App
SESSION_SECRET=
MANAGEMENT_PIN=
CHECKIN_CUTOFF=08:45
PORT=3000

# WhatsApp notifications via CallMeBot
CALLMEBOT_PHONE=
CALLMEBOT_API_KEY=

# Anthropic API — powers the telemarketer training academy's AI roleplay
# feedback (/training). Optional: without it, roleplay practice still works
# but falls back to a generic canned feedback message instead of live AI review.
ANTHROPIC_API_KEY=
